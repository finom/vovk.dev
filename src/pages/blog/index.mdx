import meta from './_meta.json';
import Link from 'next/link';
import { metadata } from './2024-05-22-vovk-2.mdx';


# Welcome to Vovk.ts Blog
 
<div>
    {console.log(metadata)}
    {Object.keys(meta).filter(key => key !== 'index' && key !== '*').map(key => (
        <div key={key}>
            {console.log(key.substring(0, 10))}
            <h2>{new Date(key.substring(0, 10)).toString()}</h2>
            <h3><Link href={`/blog/${key}`}>{meta[key].title}</Link></h3>
        </div>
    ))}
</div>