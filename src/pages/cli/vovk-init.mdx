# vovk init

If **vovk-cli** is installed globally it can be run with `vovk init` command. If it's not, it can be run with `npx vovk-cli init` command or with a shortcut `npx vovk-init`.




TODO say that dev script describes implicit/explicit way