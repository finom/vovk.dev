const Gallery = () => {
  return (
    <div>
      <h1>Gallery</h1>- Multi segment - Code generator - Customizable - Easy to distribute - Pre-validation: rpc -
      validate - fetch - validate - handle - Create client in other language
    </div>
  );
};

export default Gallery;
