```ts
import { UserRPC } from 'vovk-client';

// ...

UserRPC.createUser({
  body,
  query,
  params,
  successToast: 'Successfully created a new user',
  errorToast: 'Failed to create a new user',
  useAuth: true,
  sentryLogErrors: true,
});
```
