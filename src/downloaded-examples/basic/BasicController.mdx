```tsx filename="/src/modules/basic/BasicController.ts"
import { get, prefix } from 'vovk';

@prefix('basic')
export default class BasicController {
  /**
   * Return a greeting
   */
  @get('greeting', { cors: true })
  static getHello() {
    return { greeting: 'Hello world!' };
  }
}
```