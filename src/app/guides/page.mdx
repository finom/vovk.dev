---
asIndexPage: true
description: Nextra's File Conventions guide details the structure and organization of
  files and directories within a Nextra project, including the use of
  `page.mdx`, `_meta.js`, and `mdx-components.js` files, as well as the
  `content` and `src` directories.
---

import DocIndexCards from '../../components/DocIndexCards';
import meta, { icons } from './_meta';

# Guides

<DocIndexCards meta={meta} icons={icons} hrefPrefix="guides" />
