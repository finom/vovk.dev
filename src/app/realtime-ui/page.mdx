---
asIndexPage: true
---

import DocIndexCards from '../../components/DocIndexCards';
import meta, { icons } from './_meta';

# Realtime UI (WIP)

A practical series that walks through **Realtime Kanban**: a full‑stack reference app showing how to keep UI state consistent while updates arrive continuously (users, bots, and AI/MCP clients), without adding unnecessary complexity.

<DocIndexCards meta={meta} icons={icons} hrefPrefix="realtime-ui" />
