# Getting started

Coming soon. This article will explain `withValidationLibrary` function and controller handler properties such as `fn`, `schema` and `models`. Also common options such as `validateEachIteration`.

